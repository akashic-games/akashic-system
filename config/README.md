# jest で実行するときに使われる config

[各サーバ](../servers)で使う config は、Docker Image を作るときの default.yaml や、別リポジトリにするかも などの理由で、各サーバパッケージのディレクトリに置かれています。
そうではなくて、それらのサーバを使うクライアント側のライブラリを使った Large/Enormous テストをする場合に、各エンドポイントやアプリケーションなどで設定があり、そのために使われるものです。
